services:
  jenkins:
    container_name: jenkins
    image: jenkins/jenkins:alpine-jdk21
    ports:
      - "8085:8080"
      - "50000:50000"
    volumes:
      - jenkins_home:/var/jenkins_home
    restart: always
    networks:
      - net_jenkins
volumes:
  jenkins_home:
networks:
  net_jenkins: